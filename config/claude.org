#+TITLE: Claude
#+AUTHOR: Mathieu Marques
#+PROPERTY: header-args :mkdirp yes

* Settings

#+BEGIN_SRC json :tangle ~/.claude/settings.json
{
  "autoMemoryEnabled": false,
  "model": "claude-sonnet-5",
  "outputStyle": "Concise",
  "permissions": {
    "allow":[
      "Bash(cat:*)",
      "Bash(find:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git show:*)",
      "Bash(git status:*)",
      "Bash(grep:*)",
      "Bash(head:*)",
      "Bash(ls:*)",
      "Bash(pnpm lint*)",
      "Bash(pnpm prettier:*)",
      "Bash(pnpm test*)",
      "Bash(tail:*)",
      "Grep"
    ],
    "ask": [
      "Bash(git add:*)",
      "Bash(git commit:*)",
      "Bash(git mv:*)",
      "Bash(git push:*)",
      "Bash(git reset:*)",
      "Bash(git restore:*)",
      "Bash(git rm:*)",
      "Bash(git stage:*)",
      "Bash(git stash:*)"
    ],
    "defaultMode": "auto"
  }
}
#+END_SRC

* Instructions

#+BEGIN_SRC markdown :tangle ~/.claude/CLAUDE.md
# Response Style

- Questions should go straight to the point
- Skip preamble, answer the question directly
- Don't pad prose with headings unless the content actually has sections

# Saving Preferences

- Don't edit `~/.claude/` directly, use `~/Workspace/dot/config/claude.org`

# Coding Preferences

## TypeScript

### Documentation Strings

- Prefer one-liners if possible
- One-liners start with a capital and don't end with a dot
- Multiple lines are fine but use a single line paragraph for the summary

## Tests

- Use the tested symbol directly for describe summaries
- Start the test summary with `should`
- Structure tests with the AAA pattern i.e. `Given`, `When`, `Then`
#+END_SRC

* COMMENT Local Variables

# Local Variables:
# eval: (add-hook 'after-save-hook #'org-babel-tangle nil t)
# End:
